# uACPI test kernel

This is a kernel meant to be used to easily test uACPI on real hardware.

The kernel accepts the following arguments:
- `--uacpi-log-level=<log-level>` - allows to change the uacpi log level, can be helpful when debugging uACPI issues.
